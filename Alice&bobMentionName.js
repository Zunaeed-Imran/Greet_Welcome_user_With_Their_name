let name = prompt("What is your name?");
          if (name === "Alice" || name === "Bob") {
            alert("Hello, " + name + "!");
          } else {
            alert("Sorry, only Alice and Bob are allowed!");
          }

      